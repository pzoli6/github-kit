name: Agent Workflow Verify

on:
  pull_request:
  push:
    branches: [main, develop]

jobs:
  verify:
    uses: pzoli6/github-kit/.github/workflows/reusable-agent-workflow-verify.yml@GITHUB_KIT_VERSION
    with:
      require_claude: true
      require_cursor: true
      require_skills: true
